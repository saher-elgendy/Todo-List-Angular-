* {
    box-sizing: border-box;
}

html, body {
    margin: 0;
    padding: 0;
    font-family: 'Open Sans', sans-serif;
}

h1, input, button {
    font-family: inherit;
}

h1 {
    color: white;
    font-size: 1.6em;
}

